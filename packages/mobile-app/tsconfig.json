{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    // `vite/client` pour `import.meta.env` seul — c'est lui qui garde le simulateur
    // de reconnaissance vocale hors des builds (src/voice/index.ts).
    "types": ["vite/client"]
  },
  "include": ["src", "vite.config.ts"]
}
